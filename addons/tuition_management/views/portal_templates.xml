<?xml version="1.0" encoding="utf-8"?>
<odoo>

<!-- ═══════════════════════════════════════════════ -->
<!-- USER PROFILE: VIEW & UPDATE                     -->
<!-- ═══════════════════════════════════════════════ -->
<template id="portal_user_profile" name="Portal: User Profile">
    <t t-call="tuition_management.portal_sidebar_layout">
        
        <div class="tm-page mt-3">
            <div class="tm-hero" style="background: linear-gradient(135deg, var(--tm-indigo), var(--tm-indigo-dark));">
                <div class="d-flex flex-wrap justify-content-between align-items-center gap-3">
                    <div class="d-flex align-items-center gap-3">
                        <div class="tm-avatar" style="width:56px;height:56px;font-size:22px;background:rgba(255,255,255,0.2);color:#fff;">
                            <t t-esc="(profile.name or user.name or 'U')[:1].upper()"/>
                        </div>
                        <div>
                            <h2 class="mb-0"><t t-esc="profile.name if profile else user.name"/></h2>
                            <p class="mb-0"><span class="tm-badge" style="background:rgba(255,255,255,0.2); color:#fff;"><t t-esc="role"/></span>
                                <span style="opacity:0.7; margin-left:8px; font-size:12px;"><t t-esc="user.login"/></span>
                            </p>
                        </div>
                    </div>
                    <div>
                        <t t-if="is_tutor"><a href="/my/tutor/courses" class="tm-btn tm-btn-outline" style="background:rgba(255,255,255,0.15); color:#fff; border-color:rgba(255,255,255,0.3);"><i class="fa fa-arrow-left"/> Dashboard</a></t>
                        <t t-elif="is_student"><a href="/my/courses" class="tm-btn tm-btn-outline" style="background:rgba(255,255,255,0.15); color:#fff; border-color:rgba(255,255,255,0.3);"><i class="fa fa-arrow-left"/> Dashboard</a></t>
                        <t t-elif="is_parent"><a href="/my/parent/children" class="tm-btn tm-btn-outline" style="background:rgba(255,255,255,0.15); color:#fff; border-color:rgba(255,255,255,0.3);"><i class="fa fa-arrow-left"/> Dashboard</a></t>
                    </div>
                </div>
            </div>

            <t t-if="success">
                <div class="alert d-flex align-items-center gap-2" style="border-radius:10px; border:none; background:#EDF7ED; color:#4a8a4e;">
                    <i class="fa fa-check-circle" style="font-size:16px;"/> Profile updated successfully!
                </div>
            </t>
            <t t-if="error">
                <div class="alert d-flex align-items-center gap-2" style="border-radius:10px; border:none; background:#FDEAEA; color:#c05050;">
                    <i class="fa fa-exclamation-circle" style="font-size:16px;"/> <t t-esc="error"/>
                </div>
            </t>

            <form action="/my/profile/save" method="POST">
                <input type="hidden" name="csrf_token" t-att-value="csrf_token"/>
                <div class="row">
                    <!-- Personal Information -->
                    <div class="col-lg-6">
                        <div class="tm-card">
                            <div class="tm-card-header" style="background: linear-gradient(135deg, var(--tm-blue), var(--tm-blue-dark));">
                                <span><i class="fa fa-user me-2"/>Personal Information</span>
                            </div>
                            <div class="tm-card-body">
                                <div class="mb-3">
                                    <label class="form-label fw-bold" style="font-size:12px;">Full Name *</label>
                                    <input type="text" name="name" class="form-control" required="required"
                                           style="border-radius:8px; font-size:13px;"
                                           t-att-value="profile.name if profile else user.name"/>
                                </div>
                                <div class="mb-3">
                                    <label class="form-label fw-bold" style="font-size:12px;">Email</label>
                                    <input type="email" name="email" class="form-control"
                                           style="border-radius:8px; font-size:13px;"
                                           t-att-value="(profile.email if profile else '') or (partner.email or '')"/>
                                </div>
                                <div class="mb-3">
                                    <label class="form-label fw-bold" style="font-size:12px;">Phone</label>
                                    <input type="text" name="phone" class="form-control"
                                           style="border-radius:8px; font-size:13px;"
                                           t-att-value="(profile.phone if profile else '') or (partner.phone or '')"/>
                                </div>
                                <div class="mb-0">
                                    <div class="tm-label">Login (Username)</div>
                                    <div class="tm-value" style="font-size:13px; color:var(--tm-grey);">
                                        <i class="fa fa-lock me-1" style="font-size:11px;"/>
                                        <t t-esc="user.login"/>
                                        <small class="ms-2" style="color:var(--tm-text-light);">(cannot be changed)</small>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>

                    <!-- Change Password -->
                    <div class="col-lg-6">
                        <div class="tm-card">
                            <div class="tm-card-header" style="background: linear-gradient(135deg, var(--tm-orange), #E07040);">
                                <span><i class="fa fa-key me-2"/>Change Password</span>
                            </div>
                            <div class="tm-card-body">
                                <div class="p-2 mb-3" style="background:var(--tm-bg); border-radius:8px; font-size:12px; color:var(--tm-text-light);">
                                    <i class="fa fa-info-circle me-1"/> Leave blank to keep your current password.
                                </div>
                                <div class="mb-3">
                                    <label class="form-label fw-bold" style="font-size:12px;">New Password</label>
                                    <input type="password" name="new_password" class="form-control"
                                           style="border-radius:8px; font-size:13px;"
                                           placeholder="Min. 6 characters" autocomplete="new-password"/>
                                </div>
                                <div class="mb-3">
                                    <label class="form-label fw-bold" style="font-size:12px;">Confirm Password</label>
                                    <input type="password" name="confirm_password" class="form-control"
                                           style="border-radius:8px; font-size:13px;"
                                           placeholder="Re-enter new password" autocomplete="new-password"/>
                                </div>
                            </div>
                        </div>

                        <!-- Account Info (read-only) -->
                        <div class="tm-card">
                            <div class="tm-card-header" style="background: linear-gradient(135deg, var(--tm-grey), #607D8B);">
                                <span><i class="fa fa-info-circle me-2"/>Account Info</span>
                            </div>
                            <div class="tm-card-body">
                                <div class="tm-label">Account Type</div>
                                <div class="tm-value"><span class="tm-badge tm-badge-blue"><t t-esc="role"/></span></div>
                                <t t-if="profile">
                                    <t t-if="is_student and profile.grade_id">
                                        <div class="tm-label">Grade</div>
                                        <div class="tm-value" t-esc="profile.grade_id.name"/>
                                    </t>
                                    <t t-if="is_student and profile.parent_id">
                                        <div class="tm-label">Parent</div>
                                        <div class="tm-value" t-esc="profile.parent_id.name"/>
                                    </t>
                                    <t t-if="is_parent and profile.student_ids">
                                        <div class="tm-label">Children</div>
                                        <div class="tm-value">
                                            <t t-foreach="profile.student_ids" t-as="child">
                                                <span class="tm-badge tm-badge-teal me-1 mb-1"><t t-esc="child.name"/></span>
                                            </t>
                                        </div>
                                    </t>
                                    <t t-if="is_tutor and profile.subject_ids">
                                        <div class="tm-label">Subjects</div>
                                        <div class="tm-value">
                                            <t t-foreach="profile.subject_ids" t-as="subj">
                                                <span class="tm-badge tm-badge-purple me-1 mb-1"><t t-esc="subj.name"/></span>
                                            </t>
                                        </div>
                                    </t>
                                </t>
                            </div>
                        </div>
                    </div>
                </div>

                <div class="d-flex justify-content-end gap-2 mb-4">
                    <button type="submit" class="tm-btn tm-btn-green" style="padding: 10px 28px;">
                        <i class="fa fa-save"/> Save Changes
                    </button>
                </div>
            </form>
        </div>
    </t>
</template>

<!-- ═══════════════════════════════════════════════ -->
<!-- TUTOR: COURSES LIST (HOME)                      -->
<!-- ═══════════════════════════════════════════════ -->
<template id="portal_tutor_dashboard" name="Tutor: My Courses">
    <t t-call="tuition_management.portal_sidebar_layout">
        <t t-set="page_name">tutor_courses</t>

        <h5 style="margin:0 0 16px;font-weight:700;"><i class="fa fa-book"/> My Courses</h5>

        <t t-if="not courses">
            <div class="tm-card">
                <div class="tm-empty">
                    <i class="fa fa-book"/>
                    <p>You have no courses assigned yet.</p>
                </div>
            </div>
        </t>
        <t t-if="courses">
            <t t-foreach="courses" t-as="c">
                <div class="tm-card" style="margin-bottom:16px;">
                    <div class="tm-card-header">
                        <div class="tm-card-title">
                            <i class="fa fa-book"/>
                            <t t-esc="c.name"/>
                            <t t-if="c.subject_id">
                                <span class="tm-badge tm-badge-purple" style="font-size:11px;margin-left:8px;">
                                    <t t-esc="c.subject_id.name"/>
                                </span>
                            </t>
                            <t t-if="c.grade_id">
                                <span class="tm-badge tm-badge-blue" style="font-size:11px;margin-left:4px;">
                                    <t t-esc="c.grade_id.name"/>
                                </span>
                            </t>
                        </div>
                        <a t-attf-href="/my/tutor/courses/#{c.id}" class="tm-card-action">View Course</a>
                    </div>
                    <div class="tm-card-body">
                        <div style="display:flex;gap:24px;flex-wrap:wrap;font-size:12px;color:var(--tm-text-light);">
                            <div><i class="fa fa-users" style="margin-right:4px;"/> <strong><t t-esc="c.student_count or 0"/></strong> Students</div>
                            <div><i class="fa fa-tasks" style="margin-right:4px;"/> <strong><t t-esc="len(c.assignment_ids) if c.assignment_ids else 0"/></strong> Assignments</div>
                            <t t-if="c.start_date">
                                <div><i class="fa fa-calendar" style="margin-right:4px;"/>
                                    <t t-esc="c.start_date" t-options='{"widget": "date"}'/>
                                    <t t-if="c.end_date"> — <t t-esc="c.end_date" t-options='{"widget": "date"}'/></t>
                                </div>
                            </t>
                            <t t-if="c.virtual_class_url">
                                <a t-att-href="c.virtual_class_url" target="_blank" style="color:var(--tm-green);text-decoration:none;">
                                    <i class="fa fa-video-camera"/> Launch Class
                                </a>
                            </t>
                        </div>
                    </div>
                </div>
            </t>
        </t>
    </t>
</template>

<!-- ═══════════════════════════════════════════════ -->
<!-- ═══════════════════════════════════════════════ -->
<!-- TUTOR: COURSE DETAIL                            -->
<!-- ═══════════════════════════════════════════════ -->
<template id="portal_tutor_course_detail" name="Tutor: Course Detail">
    <t t-call="tuition_management.portal_sidebar_layout">
        <t t-set="page_name">tutor_course_detail</t>

        <div style="margin-bottom:16px;">
            <a href="/my/tutor/courses" class="tm-btn tm-btn-outline tm-btn-sm"><i class="fa fa-arrow-left"/> Back to Courses</a>
        </div>

        <!-- Course Header -->
        <div class="tm-card" style="margin-bottom:20px;">
            <div class="tm-card-body">
                <div style="display:flex;align-items:flex-start;gap:16px;flex-wrap:wrap;">
                    <div style="width:52px;height:52px;border-radius:10px;background:var(--tm-primary-light);color:var(--tm-primary);display:flex;align-items:center;justify-content:center;font-size:22px;font-weight:800;flex-shrink:0;">
                        <t t-esc="(course.name or 'C')[:1].upper()"/>
                    </div>
                    <div style="flex:1;">
                        <h2 style="font-size:18px;font-weight:700;margin:0 0 6px;"><t t-esc="course.name"/></h2>
                        <div style="font-size:13px;color:var(--tm-text-light);display:flex;gap:16px;flex-wrap:wrap;">
                            <t t-if="course.subject_id"><span><i class="fa fa-tag"/> <t t-esc="course.subject_id.name"/></span></t>
                            <t t-if="course.grade_id"><span><i class="fa fa-graduation-cap"/> <t t-esc="course.grade_id.name"/></span></t>
                            <span class="tm-badge tm-badge-green"><t t-esc="course.status or 'active'"/></span>
                        </div>
                    </div>
                    <div style="display:flex;gap:8px;flex-wrap:wrap;">
                        <t t-if="course.virtual_class_url">
                            <a t-att-href="course.virtual_class_url" target="_blank" class="tm-btn tm-btn-green"><i class="fa fa-video-camera"/> Launch Class</a>
                        </t>
                    </div>
                </div>
            </div>
        </div>

        <!-- Stat row -->
        <div class="tm-stat-grid" style="margin-bottom:20px;">
            <div class="tm-stat-card">
                <div class="tm-stat-card-top">
                    <div><div class="tm-stat-value"><t t-esc="len(enrolled_students)"/></div><div class="tm-stat-label">Students</div></div>
                    <div class="tm-stat-icon purple"><i class="fa fa-users"/></div>
                </div>
            </div>
            <div class="tm-stat-card">
                <div class="tm-stat-card-top">
                    <div><div class="tm-stat-value"><t t-esc="len(this_week_lessons)"/></div><div class="tm-stat-label">This Week's Lessons</div></div>
                    <div class="tm-stat-icon blue"><i class="fa fa-calendar"/></div>
                </div>
            </div>
            <div class="tm-stat-card">
                <div class="tm-stat-card-top">
                    <div><div class="tm-stat-value"><t t-esc="len(course_assignments)"/></div><div class="tm-stat-label">Assignments</div></div>
                    <div class="tm-stat-icon orange"><i class="fa fa-tasks"/></div>
                </div>
            </div>
            <div class="tm-stat-card">
                <div class="tm-stat-card-top">
                    <div><div class="tm-stat-value"><t t-esc="len(course_progress_reports)"/></div><div class="tm-stat-label">Progress Reports</div></div>
                    <div class="tm-stat-icon green"><i class="fa fa-line-chart"/></div>
                </div>
            </div>
        </div>

        <div class="tm-grid-2">
            <!-- Enrolled Students -->
            <div class="tm-card">
                <div class="tm-card-header"><div class="tm-card-title"><i class="fa fa-users"/> Enrolled Students</div></div>
                <t t-if="enrolled_students">
                    <table class="tm-table">
                        <thead><tr><th>Student</th><th>Grade</th></tr></thead>
                        <tbody>
                            <t t-foreach="enrolled_students" t-as="enr">
                                <tr>
                                    <td class="fw-bold"><t t-esc="enr.student_id.name"/></td>
                                    <td><t t-esc="enr.student_id.grade_id.name or '—'"/></td>
                                </tr>
                            </t>
                        </tbody>
                    </table>
                </t>
                <t t-else=""><div class="tm-empty"><i class="fa fa-users"/><p>No students enrolled.</p></div></t>
            </div>

            <!-- This Week's Lessons -->
            <div class="tm-card">
                <div class="tm-card-header">
                    <div class="tm-card-title"><i class="fa fa-calendar"/> This Week's Lessons</div>
                    <a t-attf-href="/my/tutor/courses/#{course.id}/lessons" class="tm-card-action">Full Schedule</a>
                </div>
                <t t-if="this_week_lessons">
                    <table class="tm-table">
                        <thead><tr><th>Date</th><th>Status</th><th>Attendance</th><th></th></tr></thead>
                        <tbody>
                            <t t-foreach="this_week_lessons" t-as="lesson">
                                <tr>
                                    <td style="white-space:nowrap;"><t t-esc="lesson.start_datetime" t-options='{"widget": "datetime", "format": "EEE, dd MMM"}'/></td>
                                    <td><span t-attf-class="tm-badge #{lesson.lesson_status == 'completed' and 'tm-badge-green' or lesson.lesson_status == 'cancelled' and 'tm-badge-red' or lesson.lesson_status == 'no_show' and 'tm-badge-orange' or 'tm-badge-blue'}"><t t-esc="lesson.lesson_status.capitalize()"/></span></td>
                                    <td><span t-attf-class="tm-badge #{lesson.attendance_marked and 'tm-badge-green' or 'tm-badge-grey'}"><t t-if="lesson.attendance_marked">Marked</t><t t-else="">Pending</t></span></td>
                                    <td><a t-attf-href="/my/tutor/lesson/#{lesson.id}/attendance" class="tm-btn tm-btn-outline tm-btn-sm"><i class="fa fa-check-square-o"/> <t t-if="lesson.attendance_marked">Edit</t><t t-else="">Mark</t></a></td>
                                </tr>
                            </t>
                        </tbody>
                    </table>
                </t>
                <t t-else=""><div class="tm-empty"><i class="fa fa-calendar"/><p>No lessons this week.</p></div></t>
            </div>
        </div>

        <!-- Assignments + Progress inline boxes -->
        <div class="tm-grid-2" style="margin-top:20px;">
            <!-- Assignments -->
            <div class="tm-card">
                <div class="tm-card-header">
                    <div class="tm-card-title"><i class="fa fa-tasks"/> Assignments</div>
                    <a t-attf-href="/my/tutor/courses/#{course.id}/assignments" class="tm-card-action"><i class="fa fa-plus"/> Add New</a>
                </div>
                <t t-if="course_assignments">
                    <table class="tm-table">
                        <thead><tr><th>Title</th><th>Type</th><th>Due</th><th>Submissions</th><th></th></tr></thead>
                        <tbody>
                            <t t-foreach="course_assignments[:5]" t-as="asgn">
                                <tr>
                                    <td class="fw-bold"><t t-esc="asgn.name"/></td>
                                    <td><span class="tm-badge tm-badge-grey"><t t-esc="asgn.assignment_type or '—'"/></span></td>
                                    <td style="white-space:nowrap;"><t t-if="asgn.due_date" t-esc="asgn.due_date" t-options='{"widget": "date"}'/><t t-else="">—</t></td>
                                    <td><span class="tm-badge tm-badge-blue"><t t-esc="len(asgn.submission_ids)"/></span></td>
                                    <td><a t-attf-href="/my/tutor/assignment/#{asgn.id}" class="tm-btn tm-btn-outline tm-btn-sm"><i class="fa fa-eye"/> Review</a></td>
                                </tr>
                            </t>
                        </tbody>
                    </table>
                    <t t-if="len(course_assignments) > 5">
                        <div style="padding:10px 16px;text-align:center;border-top:1px solid var(--tm-border);">
                            <a t-attf-href="/my/tutor/courses/#{course.id}/assignments" class="tm-btn tm-btn-outline tm-btn-sm">View All <t t-esc="len(course_assignments)"/> Assignments</a>
                        </div>
                    </t>
                </t>
                <t t-else="">
                    <div class="tm-empty">
                        <i class="fa fa-tasks"/>
                        <p>No assignments yet.</p>
                        <a t-attf-href="/my/tutor/courses/#{course.id}/assignments" class="tm-btn tm-btn-outline tm-btn-sm" style="margin-top:8px;"><i class="fa fa-plus"/> Create First Assignment</a>
                    </div>
                </t>
            </div>

            <!-- Progress Reports -->
            <div class="tm-card">
                <div class="tm-card-header">
                    <div class="tm-card-title"><i class="fa fa-line-chart"/> Progress Reports</div>
                    <a t-attf-href="/my/tutor/courses/#{course.id}/progress/new" class="tm-card-action"><i class="fa fa-plus"/> Add New</a>
                </div>
                <t t-if="course_progress_reports">
                    <table class="tm-table">
                        <thead><tr><th>Date</th><th>Student</th><th>Rating</th><th>Score</th></tr></thead>
                        <tbody>
                            <t t-foreach="course_progress_reports[:5]" t-as="rpt">
                                <tr>
                                    <td style="white-space:nowrap;"><t t-esc="rpt.report_date"/></td>
                                    <td class="fw-bold"><t t-esc="rpt.student_id.name"/></td>
                                    <td>
                                        <t t-if="rpt.overall_rating">
                                            <span t-attf-class="tm-badge #{rpt.overall_rating == 'excellent' and 'tm-badge-green' or rpt.overall_rating == 'good' and 'tm-badge-blue' or rpt.overall_rating == 'satisfactory' and 'tm-badge-orange' or 'tm-badge-red'}">
                                                <t t-esc="rpt.overall_rating.replace('_',' ').capitalize()"/>
                                            </span>
                                        </t>
                                        <t t-else="">—</t>
                                    </td>
                                    <td><t t-esc="rpt.score and ('%.0f / %.0f' % (rpt.score, rpt.max_score)) or '—'"/></td>
                                </tr>
                            </t>
                        </tbody>
                    </table>
                    <t t-if="len(course_progress_reports) > 5">
                        <div style="padding:10px 16px;text-align:center;border-top:1px solid var(--tm-border);">
                            <a t-attf-href="/my/tutor/courses/#{course.id}/progress" class="tm-btn tm-btn-outline tm-btn-sm">View All</a>
                        </div>
                    </t>
                </t>
                <t t-else="">
                    <div class="tm-empty">
                        <i class="fa fa-line-chart"/>
                        <p>No progress reports yet.</p>
                        <a t-attf-href="/my/tutor/courses/#{course.id}/progress/new" class="tm-btn tm-btn-outline tm-btn-sm" style="margin-top:8px;"><i class="fa fa-plus"/> Create First Report</a>
                    </div>
                </t>
            </div>
        </div>
    </t>
</template>

<!-- ═══════════════════════════════════════════════ -->
<!-- TUTOR: MARK ATTENDANCE                          -->
<!-- ═══════════════════════════════════════════════ -->
<template id="portal_tutor_attendance" name="Tutor: Mark Attendance">
    <t t-call="tuition_management.portal_sidebar_layout">
        <t t-set="page_name">tutor_course_detail</t>

        <style>
            .tm-att-radio { display:flex; gap:4px; flex-wrap:wrap; }
            .tm-att-radio label { padding:4px 10px; border-radius:6px; font-size:11px; font-weight:600; cursor:pointer; border:1.5px solid #e0e0e0; transition:all 0.15s; }
            .tm-att-radio input { display:none; }
            .tm-att-radio input[value="present"]:checked + label { background:#EDF7ED; border-color:var(--tm-success); color:#4a8a4e; }
            .tm-att-radio input[value="absent"]:checked + label { background:#FDEAEA; border-color:#e53935; color:#c05050; }
            .tm-att-radio input[value="late"]:checked + label { background:#FFF0E8; border-color:#f57c00; color:#c26030; }
            .tm-att-radio input[value="excused"]:checked + label { background:#ECEFF1; border-color:#90a4ae; color:#455A64; }
        </style>

        <div style="display:flex;justify-content:space-between;align-items:center;flex-wrap:wrap;gap:8px;margin-bottom:16px;">
            <div>
                <h5 style="margin:0 0 4px;font-weight:700;"><i class="fa fa-check-square-o"/> Mark Attendance</h5>
                <p style="margin:0;font-size:12px;color:var(--tm-text-light);"><t t-esc="occurrence.name"/> · <t t-esc="occurrence.start_datetime and occurrence.start_datetime.strftime('%a, %d %b %Y %H:%M') or ''"/></p>
            </div>
            <a t-attf-href="/my/tutor/courses/#{course.id}" class="tm-btn tm-btn-outline tm-btn-sm"><i class="fa fa-arrow-left"/> Back to Course</a>
        </div>

        <form t-attf-action="/my/tutor/lesson/#{occurrence.id}/attendance/save" method="POST">
            <input type="hidden" name="csrf_token" t-att-value="csrf_token"/>
            <div class="tm-card" style="margin-bottom:16px;">
                <div class="tm-card-header"><div class="tm-card-title"><i class="fa fa-flag"/> Lesson Status</div></div>
                <div class="tm-card-body">
                                       <div style="display:flex;align-items:center;gap:16px;flex-wrap:wrap;">
                        <select name="lesson_status" class="form-select" style="border-radius:8px;font-size:13px;max-width:220px;">
                            <option value="completed" t-att-selected="'selected' if occurrence.lesson_status == 'completed' else None">Completed</option>
                            <option value="scheduled" t-att-selected="'selected' if occurrence.lesson_status == 'scheduled' else None">Scheduled</option>
                            <option value="no_show" t-att-selected="'selected' if occurrence.lesson_status == 'no_show' else None">No Show</option>
                        </select>
                        <small style="color:var(--tm-text-light);"><i class="fa fa-info-circle"/> Only administrators can cancel a lesson.</small>
                    </div>
                </div>
            </div>
            <div class="tm-card" style="margin-bottom:16px;">
                <div class="tm-card-header"><div class="tm-card-title"><i class="fa fa-users"/> Student Attendance</div></div>
                <t t-if="not student_data"><div class="tm-empty"><i class="fa fa-users"/><p>No students enrolled.</p></div></t>
                <t t-if="student_data">
                    <table class="tm-table">
                        <thead><tr><th>Student</th><th>Status</th><th style="width:70px;">Billable</th><th>Remarks</th></tr></thead>
                        <tbody>
                            <t t-foreach="student_data" t-as="sd">
                                <tr>
                                    <td class="fw-bold"><t t-esc="sd['student'].name"/></td>
                                    <td>
                                        <div class="tm-att-radio">
                                            <input type="radio" t-attf-name="status_#{sd['student'].id}" t-attf-id="present_#{sd['student'].id}" value="present" t-att-checked="'checked' if sd['status'] == 'present' else None"/>
                                            <label t-attf-for="present_#{sd['student'].id}">Present</label>
                                            <input type="radio" t-attf-name="status_#{sd['student'].id}" t-attf-id="absent_#{sd['student'].id}" value="absent" t-att-checked="'checked' if sd['status'] == 'absent' else None"/>
                                            <label t-attf-for="absent_#{sd['student'].id}">Absent</label>
                                            <input type="radio" t-attf-name="status_#{sd['student'].id}" t-attf-id="late_#{sd['student'].id}" value="late" t-att-checked="'checked' if sd['status'] == 'late' else None"/>
                                            <label t-attf-for="late_#{sd['student'].id}">Late</label>
                                            <input type="radio" t-attf-name="status_#{sd['student'].id}" t-attf-id="excused_#{sd['student'].id}" value="excused" t-att-checked="'checked' if sd['status'] == 'excused' else None"/>
                                            <label t-attf-for="excused_#{sd['student'].id}">Excused</label>
                                        </div>
                                    </td>
                                    <td class="text-center"><input type="checkbox" t-attf-name="billable_#{sd['student'].id}" class="form-check-input" t-att-checked="'checked' if sd['billable'] else None"/></td>
                                    <td><input type="text" t-attf-name="remarks_#{sd['student'].id}" class="form-control form-control-sm" style="border-radius:6px;font-size:12px;" t-att-value="sd['remarks'] or ''"/></td>
                                </tr>
                            </t>
                        </tbody>
                    </table>
                </t>
            </div>
            <div style="display:flex;justify-content:flex-end;gap:8px;margin-bottom:24px;">
                <a t-attf-href="/my/tutor/courses/#{course.id}" class="tm-btn tm-btn-outline"><i class="fa fa-times"/> Cancel</a>
                <button type="submit" class="tm-btn tm-btn-primary"><i class="fa fa-save"/> Save Attendance</button>
            </div>
        </form>
    </t>
</template>

<!-- ═══════════════════════════════════════════════ -->
<!-- TUTOR: ASSIGNMENTS LIST + CREATE                -->
<!-- ═══════════════════════════════════════════════ -->
<template id="portal_tutor_assignments" name="Tutor: Assignments">
    <t t-call="tuition_management.portal_sidebar_layout">
        <t t-set="page_name">tutor_course_detail</t>

        <div style="display:flex;justify-content:space-between;align-items:center;flex-wrap:wrap;gap:8px;margin-bottom:16px;">
            <h5 style="margin:0;font-weight:700;"><i class="fa fa-tasks"/> <t t-esc="course.name"/> — Assignments</h5>
            <a t-attf-href="/my/tutor/courses/#{course.id}" class="tm-btn tm-btn-outline tm-btn-sm"><i class="fa fa-arrow-left"/> Back to Course</a>
        </div>

        <!-- Top stat cards -->
        <div class="tm-stat-grid" style="margin-bottom:18px;">
            <div class="tm-stat-card">
                <div class="tm-stat-card-top">
                    <div>
                        <div class="tm-stat-value"><t t-esc="len(assignments.filtered(lambda a: a.status == 'assigned')) or 0"/></div>
                        <div class="tm-stat-label">Pending</div>
                    </div>
                    <div class="tm-stat-icon orange"><i class="fa fa-clock-o"/></div>
                </div>
                <div class="tm-stat-action">Assigned to students →</div>
            </div>
            <div class="tm-stat-card">
                <div class="tm-stat-card-top">
                    <div>
                        <div class="tm-stat-value"><t t-esc="len(assignments) or 0"/></div>
                        <div class="tm-stat-label">Total</div>
                    </div>
                    <div class="tm-stat-icon purple"><i class="fa fa-list"/></div>
                </div>
                <div class="tm-stat-action">All assignments</div>
            </div>
            <div class="tm-stat-card">
                <div class="tm-stat-card-top">
                    <div>
                        <div class="tm-stat-value"><t t-esc="sum([len(a.submission_ids) for a in assignments]) or 0"/></div>
                        <div class="tm-stat-label">Submissions</div>
                    </div>
                    <div class="tm-stat-icon green"><i class="fa fa-inbox"/></div>
                </div>
                <div class="tm-stat-action">Total submissions</div>
            </div>
        </div>

        <div class="tm-grid-2">
            <!-- Create Form -->
            <div class="tm-card">
                <div class="tm-card-header"><div class="tm-card-title"><i class="fa fa-plus"/> New Assignment</div></div>
                <div class="tm-card-body">
                    <form t-attf-action="/my/tutor/courses/#{course.id}/assignments/create" method="POST" enctype="multipart/form-data">
                        <input type="hidden" name="csrf_token" t-att-value="csrf_token"/>
                        <div style="margin-bottom:12px;">
                            <label class="form-label">Title *</label>
                            <input type="text" name="name" class="form-control form-control-sm" required="required" placeholder="e.g. Chapter 5 Worksheet"/>
                        </div>
                        <div style="margin-bottom:12px;">
                            <label class="form-label">Type</label>
                            <select name="assignment_type" class="form-select form-select-sm">
                                <option value="homework">Homework</option>
                                <option value="quiz">Quiz</option>
                                <option value="test">Test</option>
                                <option value="project">Project</option>
                                <option value="worksheet">Worksheet</option>
                                <option value="other">Other</option>
                            </select>
                        </div>
                        <div style="margin-bottom:12px;">
                            <label class="form-label">Due Date</label>
                            <input type="date" name="due_date" class="form-control form-control-sm"/>
                        </div>
                        <div style="margin-bottom:12px;">
                            <label class="form-label">Description</label>
                            <textarea name="description" class="form-control form-control-sm" rows="3" placeholder="Instructions..."/>
                        </div>
                        <div style="margin-bottom:16px;">
                            <label class="form-label">Attachment</label>
                            <input type="file" name="attachment" class="form-control form-control-sm"/>
                        </div>
                        <button type="submit" class="tm-btn tm-btn-primary" style="width:100%;justify-content:center;"><i class="fa fa-plus"/> Create</button>
                    </form>
                </div>
            </div>

            <!-- Assignments List -->
            <div class="tm-card">
                <div class="tm-card-header"><div class="tm-card-title"><i class="fa fa-list"/> All Assignments</div></div>
                <div class="tm-card-body p-0">
                    <t t-if="not assignments"><div class="tm-empty"><i class="fa fa-tasks"/><p>No assignments yet.</p></div></t>
                    <t t-if="assignments">
                        <div class="table-responsive">
                            <table class="tm-table">
                                <thead>
                                    <tr>
                                        <th>Title</th>
                                        <th>Type</th>
                                        <th>Due</th>
                                        <th>Submissions</th>
                                        <th></th>
                                    </tr>
                                </thead>
                                <tbody>
                                    <t t-foreach="assignments" t-as="asgn">
                                        <tr>
                                            <td class="fw-bold"><t t-esc="asgn.name"/></td>
                                            <td><span class="tm-badge tm-badge-grey"><t t-esc="asgn.assignment_type or '—'"/></span></td>
                                            <td style="white-space:nowrap;"><t t-if="asgn.due_date" t-esc="asgn.due_date" t-options='{"widget": "date"}'/><t t-else="">—</t></td>
                                            <td><span class="tm-badge tm-badge-purple"><t t-esc="len(asgn.submission_ids)"/></span></td>
                                            <td><a t-attf-href="/my/tutor/assignment/#{asgn.id}" class="tm-btn tm-btn-outline tm-btn-sm"><i class="fa fa-eye"/> Review</a></td>
                                        </tr>
                                    </t>
                                </tbody>
                            </table>
                        </div>
                    </t>
                </div>
            </div>
        </div>
    </t>
</template>

<!-- ═══════════════════════════════════════════════ -->
<!-- TUTOR: ASSIGNMENT DETAIL + GRADE                -->
<!-- ═══════════════════════════════════════════════ -->
<template id="portal_tutor_assignment_detail" name="Tutor: Assignment Detail">
    <t t-call="tuition_management.portal_sidebar_layout">
        
        <div class="tm-page mt-3">
            <div class="d-flex flex-wrap justify-content-between align-items-center mb-3 gap-2">
                <h5 class="mb-0 fw-bold" style="color:var(--tm-teal);"><i class="fa fa-file-text me-2"/><t t-esc="assignment.name"/></h5>
                <a t-attf-href="/my/tutor/courses/#{course.id}/assignments" class="tm-btn tm-btn-outline tm-btn-sm"><i class="fa fa-arrow-left"/> Assignments</a>
            </div>
            <t t-if="request.params.get('graded')">
                <div class="alert alert-success d-flex align-items-center gap-2" style="border-radius:10px; border:none; background:#EDF7ED; color:#4a8a4e;"><i class="fa fa-check-circle"/> Graded!</div>
            </t>
            <div class="row">
                <div class="col-lg-4">
                    <div class="tm-card">
                        <div class="tm-card-header" style="background: linear-gradient(135deg, var(--tm-teal), var(--tm-teal-dark));"><span><i class="fa fa-info-circle me-2"/>Details</span></div>
                        <div class="tm-card-body">
                            <div class="tm-label">Course</div><div class="tm-value" t-esc="course.name"/>
                            <div class="tm-label">Type</div><div class="tm-value" t-esc="dict(assignment._fields['assignment_type'].selection).get(assignment.assignment_type, '')"/>
                            <div class="tm-label">Status</div><div class="tm-value">
                                <span t-if="assignment.status == 'draft'" class="tm-badge tm-badge-grey">Draft</span>
                                <span t-if="assignment.status == 'assigned'" class="tm-badge tm-badge-blue">Assigned</span>
                                <span t-if="assignment.status == 'completed'" class="tm-badge tm-badge-green">Completed</span>
                            </div>
                            <t t-if="assignment.due_date"><div class="tm-label">Due Date</div><div class="tm-value fw-bold" style="color:var(--tm-red);" t-esc="assignment.due_date" t-options='{"widget": "date"}'/></t>
                            <t t-if="assignment.description"><div class="tm-label">Description</div><div class="tm-value" t-raw="assignment.description"/></t>
                            <t t-if="assignment.attachment_ids"><div class="tm-label">Attachments</div><t t-foreach="assignment.attachment_ids" t-as="att"><a t-attf-href="/web/content/#{att.id}?download=true" class="tm-file-link"><i class="fa fa-download"/> <t t-esc="att.name"/></a></t></t>
                        </div>
                    </div>
                </div>
                <div class="col-lg-8">
                    <div class="tm-card">
                        <div class="tm-card-header" style="background: linear-gradient(135deg, var(--tm-green), var(--tm-green-dark));"><span><i class="fa fa-inbox me-2"/>Submissions (<t t-esc="len(submissions)"/>)</span></div>
                        <t t-if="not submissions"><div class="tm-empty"><i class="fa fa-inbox"/><p>No submissions yet</p></div></t>
                        <t t-if="submissions">
                            <t t-foreach="submissions" t-as="sub">
                                <div class="px-3 py-3" style="border-bottom: 1px solid #f0f0f0;">
                                    <div class="d-flex flex-wrap justify-content-between align-items-start gap-2 mb-2">
                                        <div class="d-flex align-items-center gap-2">
                                            <div class="tm-avatar tm-avatar-green" style="width:32px;height:32px;font-size:12px;"><t t-esc="sub.student_id.name[:1].upper()"/></div>
                                            <div>
                                                <div class="fw-bold" style="font-size:12px;" t-esc="sub.student_id.name"/>
                                                <div style="font-size:10px; color:var(--tm-text-light);">
                                                    <t t-esc="sub.submission_date" t-options='{"widget": "datetime", "format": "dd MMM yyyy hh:mm a"}'/>
                                                </div>
                                            </div>
                                        </div>
                                        <span t-if="sub.status == 'submitted'" class="tm-badge tm-badge-green">Submitted</span>
                                        <span t-if="sub.status == 'graded'" class="tm-badge tm-badge-green">Graded</span>
                                        <span t-if="sub.status == 'pending'" class="tm-badge tm-badge-orange">Pending</span>
                                    </div>
                                    <t t-if="sub.attachment_ids">
                                        <div class="mb-2"><t t-foreach="sub.attachment_ids" t-as="att"><a t-attf-href="/web/content/#{att.id}?download=true" class="tm-file-link"><i class="fa fa-paperclip"/> <t t-esc="att.name"/></a></t></div>
                                    </t>
                                    <t t-if="sub.status == 'graded' and sub.feedback">
                                        <div class="p-2 mb-2" style="background:var(--tm-bg); border-radius:8px; font-size:11px;"><span class="fw-bold">Feedback:</span> <t t-esc="sub.feedback"/></div>
                                    </t>
                                    <form t-attf-action="/my/tutor/submission/#{sub.id}/grade" method="POST" class="mt-2">
                                        <input type="hidden" name="csrf_token" t-att-value="csrf_token"/>
                                        <div class="row g-2 align-items-end">
                                            <div class="col-3">
                                                <label class="form-label fw-bold" style="font-size:10px;">Score</label>
                                                <div class="input-group input-group-sm">
                                                    <input type="number" name="score" class="form-control" step="0.1" min="0" t-att-max="sub.max_score" t-att-value="sub.score or ''" style="border-radius:6px 0 0 6px; font-size:11px;"/>
                                                    <span class="input-group-text" style="font-size:10px;">/<t t-esc="int(sub.max_score)"/></span>
                                                </div>
                                            </div>
                                            <div class="col-6"><label class="form-label fw-bold" style="font-size:10px;">Feedback</label><input type="text" name="feedback" class="form-control form-control-sm" style="border-radius:6px; font-size:11px;" t-att-value="sub.feedback or ''" placeholder="Good work!"/></div>
                                            <div class="col-3"><button type="submit" class="tm-btn tm-btn-green tm-btn-sm w-100 justify-content-center"><i class="fa fa-check"/><t t-if="sub.status == 'graded'">Update</t><t t-else="">Grade</t></button></div>
                                        </div>
                                    </form>
                                </div>
                            </t>
                        </t>
                    </div>
                </div>
            </div>
        </div>
    </t>
</template>

<template id="portal_tutor_lessons" name="Tutor: Lessons">
    <t t-call="tuition_management.portal_sidebar_layout">
        <t t-set="page_name">tutor_course_detail</t>

        <div style="display:flex;justify-content:space-between;align-items:center;flex-wrap:wrap;gap:8px;margin-bottom:16px;">
            <h5 style="margin:0;font-weight:700;"><i class="fa fa-calendar"/> <t t-esc="course.name"/> — Schedule</h5>
            <a t-att-href="back_url" class="tm-btn tm-btn-outline tm-btn-sm"><i class="fa fa-arrow-left"/> Back to Course</a>
        </div>

        <div style="margin-bottom:16px;display:flex;gap:8px;flex-wrap:wrap;">
            <a t-attf-href="/my/tutor/courses/#{course.id}/lessons?week=this" t-attf-class="tm-btn tm-btn-sm #{current_week == 'this' and 'tm-btn-primary' or 'tm-btn-outline'}">This Week</a>
            <a t-attf-href="/my/tutor/courses/#{course.id}/lessons?week=next" t-attf-class="tm-btn tm-btn-sm #{current_week == 'next' and 'tm-btn-primary' or 'tm-btn-outline'}">Next Week</a>
            <a t-attf-href="/my/tutor/courses/#{course.id}/lessons?week=all" t-attf-class="tm-btn tm-btn-sm #{current_week == 'all' and 'tm-btn-primary' or 'tm-btn-outline'}">All Upcoming</a>
            <a t-attf-href="/my/tutor/courses/#{course.id}/lessons?week=past" t-attf-class="tm-btn tm-btn-sm #{current_week == 'past' and 'tm-btn-primary' or 'tm-btn-outline'}">Past (30 days)</a>
        </div>

        <div class="tm-card">
            <div class="tm-card-header"><div class="tm-card-title"><i class="fa fa-list"/> Schedule</div></div>
            <t t-if="not occ_data"><div class="tm-empty"><i class="fa fa-calendar"/><p>No lessons found for this period.</p></div></t>
            <t t-if="occ_data">
                <table class="tm-table">
                    <thead><tr><th>Date</th><th>Time</th><th>Status</th><th>Attendance</th><th></th></tr></thead>
                    <tbody>
                        <t t-foreach="occ_data" t-as="od">
                            <tr>
                                <td class="fw-bold" style="white-space:nowrap;"><t t-esc="od['date_str']"/></td>
                                <td><t t-esc="od['time_str']"/></td>
                                <td><span t-attf-class="tm-badge #{od['occ'].lesson_status == 'completed' and 'tm-badge-green' or od['occ'].lesson_status == 'cancelled' and 'tm-badge-red' or od['occ'].lesson_status == 'no_show' and 'tm-badge-orange' or 'tm-badge-blue'}"><t t-esc="od['occ'].lesson_status.capitalize()"/></span></td>
                                <td><span t-attf-class="tm-badge #{od['occ'].attendance_marked and 'tm-badge-green' or 'tm-badge-grey'}"><t t-if="od['occ'].attendance_marked">Marked</t><t t-else="">Pending</t></span></td>
                                <td><a t-attf-href="/my/tutor/lesson/#{od['occ'].id}/attendance" class="tm-btn tm-btn-outline tm-btn-sm"><i class="fa fa-check-square-o"/> <t t-if="od['occ'].attendance_marked">Edit</t><t t-else="">Mark</t></a></td>
                            </tr>
                        </t>
                    </tbody>
                </table>
            </t>
        </div>
    </t>
</template>

<template id="portal_parent_invoices" name="Parent: Invoices">
    <t t-call="tuition_management.portal_sidebar_layout">
        <t t-set="page_name">tuition_invoices</t>
        <t t-set="page_title">Invoices</t>

        <h5 style="margin:0 0 16px;font-weight:700;"><i class="fa fa-file-text-o"/> Invoices</h5>

        <!-- Summary Cards -->
        <div class="tm-stat-grid" style="margin-bottom:20px;">
            <div class="tm-stat-card">
                <div class="tm-stat-card-top">
                    <div>
                        <div class="tm-stat-value"><t t-esc="len(invoices)"/></div>
                        <div class="tm-stat-label">Total Invoices</div>
                    </div>
                    <div class="tm-stat-icon blue"><i class="fa fa-file-text-o"/></div>
                </div>
            </div>
            <div class="tm-stat-card">
                <div class="tm-stat-card-top">
                    <div>
                        <div class="tm-stat-value" style="color:var(--tm-green);"><t t-esc="len(invoices.filtered(lambda i: i.payment_state == 'paid'))"/></div>
                        <div class="tm-stat-label">Paid</div>
                    </div>
                    <div class="tm-stat-icon green"><i class="fa fa-check-circle"/></div>
                </div>
            </div>
            <div class="tm-stat-card">
                <div class="tm-stat-card-top">
                    <div>
                        <div class="tm-stat-value" style="color:var(--tm-orange);"><t t-esc="len(invoices.filtered(lambda i: i.payment_state != 'paid' and i.state == 'posted'))"/></div>
                        <div class="tm-stat-label">Unpaid</div>
                    </div>
                    <div class="tm-stat-icon orange"><i class="fa fa-clock-o"/></div>
                </div>
            </div>
        </div>

        <t t-if="not invoices">
            <div class="tm-card">
                <div class="tm-empty">
                    <i class="fa fa-file-o"/>
                    <p>No invoices found.</p>
                </div>
            </div>
        </t>
        <t t-if="invoices">
            <div class="tm-card">
                <div class="tm-card-header">
                    <div class="tm-card-title"><i class="fa fa-list"/> All Invoices</div>
                </div>
                <div class="table-responsive">
                    <table class="tm-table">
                        <thead>
                            <tr>
                                <th>Invoice #</th>
                                <th>Date</th>
                                <th>Due Date</th>
                                <th>Student</th>
                                <th style="text-align:right;">Amount</th>
                                <th>Status</th>
                                <th></th>
                            </tr>
                        </thead>
                        <tbody>
                            <t t-foreach="invoices" t-as="inv">
                                <tr>
                                    <td class="fw-bold"><t t-esc="inv.name"/></td>
                                    <td style="white-space:nowrap;">
                                        <t t-if="inv.invoice_date" t-esc="inv.invoice_date" t-options='{"widget": "date"}'/>
                                        <t t-else="">—</t>
                                    </td>
                                    <td style="white-space:nowrap;">
                                        <t t-if="inv.invoice_date_due">
                                            <span style="color:var(--tm-orange);font-weight:600;"><t t-esc="inv.invoice_date_due" t-options='{"widget": "date"}'/></span>
                                        </t>
                                        <t t-else="">—</t>
                                    </td>
                                    <td>
                                        <t t-if="inv.tuition_subscription_id and inv.tuition_subscription_id.student_id">
                                            <span class="tm-badge tm-badge-purple" style="font-size:10px;"><t t-esc="inv.tuition_subscription_id.student_id.name"/></span>
                                        </t>
                                        <t t-else="">—</t>
                                    </td>
                                    <td style="text-align:right;font-weight:600;">
                                        <t t-esc="'%.2f' % inv.amount_total"/> <t t-esc="inv.currency_id.symbol"/>
                                    </td>
                                    <td>
                                        <span t-if="inv.payment_state == 'paid'" class="tm-badge tm-badge-green">Paid</span>
                                        <span t-elif="inv.payment_state == 'partial'" class="tm-badge tm-badge-orange">Partial</span>
                                        <span t-elif="inv.state == 'posted'" class="tm-badge tm-badge-red">Unpaid</span>
                                        <span t-elif="inv.state == 'draft'" class="tm-badge tm-badge-grey">Draft</span>
                                        <span t-else="" class="tm-badge tm-badge-grey"><t t-esc="inv.state"/></span>
                                    </td>
                                    <td>
                                        <a t-attf-href="/my/invoices/#{inv.id}" class="tm-btn tm-btn-outline tm-btn-sm">
                                            <i class="fa fa-eye"/> View
                                        </a>
                                    </td>
                                </tr>
                            </t>
                        </tbody>
                    </table>
                </div>
            </div>
        </t>
    </t>
</template>

<!-- ═══════════════════════════════════════════════ -->
<!-- PARENT: INVOICE DETAIL                          -->
<!-- ═══════════════════════════════════════════════ -->
<template id="portal_parent_invoice_detail" name="Parent: Invoice Detail">
    <t t-call="tuition_management.portal_sidebar_layout">
        <t t-set="page_name">tuition_invoices</t>
        <t t-set="page_title">Invoice</t>

        <div style="margin-bottom:16px;">
            <a href="/my/tuition/invoices" class="tm-btn tm-btn-outline tm-btn-sm"><i class="fa fa-arrow-left"/> Back to Invoices</a>
        </div>

        <!-- Invoice Header Card -->
        <div class="tm-card" style="margin-bottom:20px;">
            <div class="tm-card-body">
                <div style="display:flex;align-items:flex-start;gap:16px;flex-wrap:wrap;justify-content:space-between;">
                    <div style="display:flex;align-items:center;gap:16px;">
                        <div style="width:52px;height:52px;border-radius:10px;background:var(--tm-primary-light);color:var(--tm-primary);display:flex;align-items:center;justify-content:center;font-size:22px;flex-shrink:0;">
                            <i class="fa fa-file-text-o"/>
                        </div>
                        <div>
                            <h2 style="font-size:18px;font-weight:700;margin:0 0 6px;"><t t-esc="invoice.name"/></h2>
                            <div style="font-size:13px;color:var(--tm-text-light);display:flex;gap:16px;flex-wrap:wrap;">
                                <t t-if="invoice.invoice_date">
                                    <span><i class="fa fa-calendar"/> <t t-esc="invoice.invoice_date" t-options='{"widget": "date"}'/></span>
                                </t>
                                <t t-if="invoice.invoice_date_due">
                                    <span><i class="fa fa-clock-o"/> Due: <t t-esc="invoice.invoice_date_due" t-options='{"widget": "date"}'/></span>
                                </t>
                                <t t-if="invoice.tuition_subscription_id and invoice.tuition_subscription_id.student_id">
                                    <span><i class="fa fa-child"/> <t t-esc="invoice.tuition_subscription_id.student_id.name"/></span>
                                </t>
                                <span t-if="invoice.payment_state == 'paid'" class="tm-badge tm-badge-green">Paid</span>
                                <span t-elif="invoice.payment_state == 'partial'" class="tm-badge tm-badge-orange">Partially Paid</span>
                                <span t-elif="invoice.state == 'posted'" class="tm-badge tm-badge-red">Unpaid</span>
                                <span t-elif="invoice.state == 'draft'" class="tm-badge tm-badge-grey">Draft</span>
                                <span t-elif="invoice.state == 'cancel'" class="tm-badge tm-badge-red">Cancelled</span>
                                <span t-else="" class="tm-badge tm-badge-grey"><t t-esc="invoice.state"/></span>
                            </div>
                        </div>
                    </div>
                    <div style="display:flex;gap:8px;flex-wrap:wrap;">
                        <t t-if="invoice.virtual_class_url">
                            <a t-att-href="invoice.virtual_class_url" target="_blank" class="tm-btn tm-btn-green"><i class="fa fa-video-camera"/> Launch Class</a>
                        </t>
                    </div>
                </div>
            </div>
        </div>

        <!-- Invoice Details -->
        <div class="tm-card" style="margin-bottom:20px;">
            <div class="tm-card-header">
                <div class="tm-card-title"><i class="fa fa-info-circle"/> Invoice Details</div>
            </div>
            <div class="tm-card-body">
                <div class="tm-label">Invoice Number</div>
                <div class="tm-value fw-bold"><t t-esc="invoice.name"/></div>

                <div class="tm-label">Invoice Date</div>
                <div class="tm-value"><t t-if="invoice.invoice_date" t-esc="invoice.invoice_date" t-options='{"widget": "date"}'/><t t-else="">—</t></div>

                <div class="tm-label">Due Date</div>
                <div class="tm-value">
                    <t t-if="invoice.invoice_date_due">
                        <span style="font-weight:600;color:var(--tm-orange);"><t t-esc="invoice.invoice_date_due" t-options='{"widget": "date"}'/></span>
                    </t>
                    <t t-else="">—</t>
                </div>

                <t t-if="invoice.ref">
                    <div class="tm-label">Reference</div>
                    <div class="tm-value"><t t-esc="invoice.ref"/></div>
                </t>

                <t t-if="invoice.invoice_origin">
                    <div class="tm-label">Source</div>
                    <div class="tm-value"><t t-esc="invoice.invoice_origin"/></div>
                </t>
            </div>
        </div>

        <!-- Billing Info -->
        <div class="tm-card" style="margin-bottom:20px;">
            <div class="tm-card-header">
                <div class="tm-card-title"><i class="fa fa-user"/> Billing Information</div>
            </div>
            <div class="tm-card-body">
                <div class="tm-label">Billed To</div>
                <div class="tm-value fw-bold"><t t-esc="invoice.partner_id.name"/></div>

                <t t-if="invoice.partner_id.email">
                    <div class="tm-label">Email</div>
                    <div class="tm-value"><t t-esc="invoice.partner_id.email"/></div>
                </t>

                <t t-if="invoice.partner_id.phone">
                    <div class="tm-label">Phone</div>
                    <div class="tm-value"><t t-esc="invoice.partner_id.phone"/></div>
                </t>

                <t t-if="invoice.tuition_subscription_id">
                    <div class="tm-label">Student</div>
                    <div class="tm-value">
                        <span class="tm-badge tm-badge-purple">
                            <t t-esc="invoice.tuition_subscription_id.student_id.name"/>
                        </span>
                    </div>

                    <t t-if="invoice.tuition_subscription_id.current_plan_product">
                        <div class="tm-label">Plan</div>
                        <div class="tm-value"><t t-esc="invoice.tuition_subscription_id.current_plan_product"/></div>
                    </t>
                </t>
            </div>
        </div>

        <!-- Invoice Lines -->
        <div class="tm-card" style="margin-bottom:20px;">
            <div class="tm-card-header">
                <div class="tm-card-title"><i class="fa fa-list"/> Invoice Lines</div>
            </div>
            <table class="tm-table">
                <thead>
                    <tr>
                        <th>Description</th>
                        <th style="text-align:right;">Qty</th>
                        <th style="text-align:right;">Unit Price</th>
                        <th style="text-align:right;">Subtotal</th>
                    </tr>
                </thead>
                <tbody>
                    <t t-foreach="invoice.invoice_line_ids.filtered(lambda l: not l.display_type)" t-as="line">
                        <tr>
                            <td class="fw-bold"><t t-esc="line.name or line.product_id.name or '—'"/></td>
                            <td style="text-align:right;"><t t-esc="'%.2f' % line.quantity"/></td>
                            <td style="text-align:right;"><t t-esc="'%.2f' % line.price_unit"/> <t t-esc="invoice.currency_id.symbol"/></td>
                            <td style="text-align:right;font-weight:600;"><t t-esc="'%.2f' % line.price_subtotal"/> <t t-esc="invoice.currency_id.symbol"/></td>
                        </tr>
                    </t>
                </tbody>
            </table>
        </div>

        <!-- Totals + Payment -->
        <div class="tm-card" style="margin-bottom:20px;">
            <div class="tm-card-header">
                <div class="tm-card-title"><i class="fa fa-calculator"/> Summary</div>
            </div>
            <div class="tm-card-body">
                <div style="max-width:380px;margin-left:auto;">
                    <div style="display:flex;justify-content:space-between;padding:8px 0;font-size:13px;border-bottom:1px solid var(--tm-border);">
                        <span style="color:var(--tm-text-light);">Untaxed Amount</span>
                        <span><t t-esc="'%.2f' % invoice.amount_untaxed"/> <t t-esc="invoice.currency_id.symbol"/></span>
                    </div>
                    <t t-if="invoice.amount_tax">
                        <div style="display:flex;justify-content:space-between;padding:8px 0;font-size:13px;border-bottom:1px solid var(--tm-border);">
                            <span style="color:var(--tm-text-light);">Tax</span>
                            <span><t t-esc="'%.2f' % invoice.amount_tax"/> <t t-esc="invoice.currency_id.symbol"/></span>
                        </div>
                    </t>
                    <div style="display:flex;justify-content:space-between;padding:12px 0;font-size:16px;font-weight:700;border-bottom:1px solid var(--tm-border);">
                        <span>Total</span>
                        <span><t t-esc="'%.2f' % invoice.amount_total"/> <t t-esc="invoice.currency_id.symbol"/></span>
                    </div>
                    <t t-if="invoice.payment_state == 'paid'">
                        <div style="display:flex;justify-content:space-between;padding:12px 0;font-size:14px;font-weight:600;color:var(--tm-green);">
                            <span><i class="fa fa-check-circle"/> Paid</span>
                            <span><t t-esc="'%.2f' % invoice.amount_total"/> <t t-esc="invoice.currency_id.symbol"/></span>
                        </div>
                    </t>
                    <t t-if="invoice.payment_state != 'paid' and invoice.state == 'posted'">
                        <div style="display:flex;justify-content:space-between;padding:12px 0;font-size:14px;font-weight:600;color:var(--tm-orange);">
                            <span>Amount Due</span>
                            <span><t t-esc="'%.2f' % invoice.amount_residual"/> <t t-esc="invoice.currency_id.symbol"/></span>
                        </div>
                        <div style="padding:12px 0;text-align:right;">
                            <a t-attf-href="/my/invoices/#{invoice.id}" class="tm-btn tm-btn-green" style="padding:10px 28px;">
                                <i class="fa fa-credit-card"/> Pay Now
                            </a>
                        </div>
                    </t>
                </div>
            </div>
        </div>

        <t t-if="invoice.narration">
            <div class="tm-card" style="margin-bottom:20px;">
                <div class="tm-card-header">
                    <div class="tm-card-title"><i class="fa fa-sticky-note"/> Notes</div>
                </div>
                <div class="tm-card-body">
                    <div style="font-size:13px;" t-raw="invoice.narration"/>
                </div>
            </div>
        </t>
    </t>
</template>

</odoo>
