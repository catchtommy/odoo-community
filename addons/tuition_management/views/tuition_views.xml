<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Enquiry Views -->
    <record id="view_enquiry_form" model="ir.ui.view">
        <field name="name">enquiry.form</field>
        <field name="model">enquiry</field>
        <field name="arch" type="xml">
            <form string="Enquiry">
                <header>
                    <field name="is_enrolled_stage" invisible="1"/>
                    <field name="is_enrolled" invisible="1"/>
                    <button name="action_enroll_to_course" type="object" string="Convert to Course" class="btn-primary" icon="fa-graduation-cap" invisible="not is_enrolled_stage or is_enrolled"/>
                </header>
                <div class="alert alert-success" role="status" invisible="not is_enrolled">
                    <strong><i class="fa fa-check-circle"/> Enrolled!</strong> This enquiry has been converted to a course enrollment.
                </div>
                <div class="alert alert-info" role="status">
                    <field name="stage_id" nolabel="1" widget="statusbar" options="{'clickable': True}" readonly="is_enrolled"/>
                </div>
                <sheet>
                    <div class="oe_button_box" name="button_box">
                        <button name="action_delete_enquiry" type="object" class="oe_stat_button" icon="fa-trash" invisible="not id or is_enrolled" confirm="Are you sure you want to permanently delete this enquiry?">
                            <span>Discard</span>
                        </button>
                    </div>
                    <group string="Course Information" invisible="not is_enrolled">
                        <field name="course_id"/><field name="enrollment_id"/>
                        <field name="parent_profile_id"/><field name="student_profile_id"/>
                    </group>
                    <group string="Enquiry Information">
                        <field name="enquiry_name" readonly="1"/>
                        <field name="enquiry_date" readonly="is_enrolled"/>
                    </group>
                    <group string="Parent Information">
                        <field name="name" string="Parent Name" readonly="is_enrolled"/>
                        <field name="email" readonly="is_enrolled"/>
                        <field name="partner_id" readonly="is_enrolled"/>
                        <label for="phone" string="Phone Number"/>
                        <div style="display: grid; grid-template-columns: 25% 75%; gap: 8px;">
                            <field name="country_code" placeholder="Country Code" readonly="is_enrolled"/>
                            <field name="phone" class="o_field_char" placeholder="Phone Number" readonly="is_enrolled"/>
                        </div>
                    </group>
                    <group string="Student Information">
                        <field name="student_name" readonly="is_enrolled"/>
                    </group>
                    <group string="Academic Information">
                        <field name="subject_id" readonly="is_enrolled"/>
                        <field name="grade_id" readonly="is_enrolled"/>
                    </group>
                    <group string="Demo Sessions">
                        <field name="demo_session_ids" readonly="is_enrolled"/>
                    </group>
                    <group string="Notes">
                        <field name="notes" nolabel="1" readonly="is_enrolled"/>
                    </group>
                </sheet>
            </form>
        </field>
    </record>
    <record id="view_enquiry_list" model="ir.ui.view">
        <field name="name">enquiry.list</field>
        <field name="model">enquiry</field>
        <field name="arch" type="xml">
            <list string="Enquiries">
                <field name="enquiry_name"/><field name="name" string="Parent Name"/>
                <field name="student_name"/><field name="email"/><field name="phone"/>
                <field name="subject_id"/><field name="grade_id"/><field name="stage_id"/>
            </list>
        </field>
    </record>
    <record id="view_enquiry_kanban" model="ir.ui.view">
        <field name="name">enquiry.kanban</field>
        <field name="model">enquiry</field>
        <field name="arch" type="xml">
            <kanban default_group_by="stage_id" class="o_kanban_small_column" quick_create="false">
                <field name="enquiry_name"/><field name="name"/><field name="student_name"/>
                <field name="email"/><field name="phone"/><field name="subject_id"/>
                <field name="grade_id"/><field name="stage_id"/><field name="enquiry_date"/>
                <templates>
                    <t t-name="card">
                        <div class="oe_kanban_content">
                            <div class="o_kanban_record_top mb-0">
                                <div class="o_kanban_record_headings">
                                    <strong class="o_kanban_record_title"><field name="enquiry_name"/></strong>
                                </div>
                            </div>
                            <div class="o_kanban_record_body">
                                <div class="mb-1"><strong>Parent:</strong> <field name="name"/></div>
                                <div class="mb-1"><strong>Student:</strong> <field name="student_name"/></div>
                                <div class="mb-1" t-if="record.subject_id.value"><strong>Subject:</strong> <field name="subject_id"/></div>
                                <div class="mb-1" t-if="record.grade_id.value"><strong>Grade:</strong> <field name="grade_id"/></div>
                                <div class="text-muted" t-if="record.email.value"><i class="fa fa-envelope me-1"/><field name="email"/></div>
                                <div class="text-muted" t-if="record.phone.value"><i class="fa fa-phone me-1"/><field name="phone"/></div>
                            </div>
                            <div class="o_kanban_record_bottom">
                                <div class="oe_kanban_bottom_left"><field name="enquiry_date" widget="date"/></div>
                            </div>
                        </div>
                    </t>
                </templates>
            </kanban>
        </field>
    </record>
    <record id="view_enquiry_form_quick_create" model="ir.ui.view">
        <field name="name">enquiry.form.quick_create</field>
        <field name="model">enquiry</field>
        <field name="arch" type="xml">
            <form>
                <group>
                    <field name="enquiry_name" placeholder="e.g., SAT Enquiry for John"/>
                    <field name="name" string="Parent Name" placeholder="Parent Full Name"/>
                    <field name="student_name" placeholder="Student Full Name"/>
                    <field name="partner_id"/><field name="email" placeholder="Email"/>
                    <field name="phone" placeholder="Phone"/>
                    <field name="subject_id"/><field name="grade_id"/>
                </group>
            </form>
        </field>
    </record>
    <record id="action_enquiry" model="ir.actions.act_window">
        <field name="name">Enquiries</field>
        <field name="res_model">enquiry</field>
        <field name="view_mode">kanban,list,form</field>
    </record>

    <!-- Demo Session Views -->
    <record id="view_demo_session_list" model="ir.ui.view">
        <field name="name">demo.session.list</field>
        <field name="model">demo.session</field>
        <field name="arch" type="xml">
            <list string="Demo Sessions">
                <field name="enquiry_id"/><field name="subject_id"/><field name="tutor_id"/>
                <field name="scheduled_datetime"/><field name="duration_minutes"/>
                <field name="status"/><field name="rating"/>
            </list>
        </field>
    </record>
    <record id="view_demo_session_form" model="ir.ui.view">
        <field name="name">demo.session.form</field>
        <field name="model">demo.session</field>
        <field name="arch" type="xml">
            <form string="Demo Session">
                <sheet>
                    <group>
                        <field name="enquiry_id"/><field name="subject_id"/><field name="tutor_id"/>
                    </group>
                    <group>
                        <field name="scheduled_datetime"/><field name="timezone"/>
                        <field name="duration_minutes" string="Duration (Minutes)"/>
                    </group>
                    <group>
                        <field name="status"/><field name="rating"/>
                    </group>
                    <group string="Feedback">
                        <field name="feedback" nolabel="1" widget="text" colspan="2" style="width: 100%; min-height: 200px;"/>
                    </group>
                </sheet>
            </form>
        </field>
    </record>
    <record id="action_demo_sessions" model="ir.actions.act_window">
        <field name="name">Demo Sessions</field>
        <field name="res_model">demo.session</field>
        <field name="view_mode">list,form</field>
    </record>

    <!-- Subscription Invoice Wizards -->
    <record id="view_tuition_invoice_preview_wizard_form" model="ir.ui.view">
        <field name="name">tuition.invoice.preview.wizard.form</field>
        <field name="model">tuition.invoice.preview.wizard</field>
        <field name="arch" type="xml">
            <form string="Invoice Preview">
                <group>
                    <field name="partner_id" readonly="1"/><field name="month_label" readonly="1"/>
                    <field name="base_price" readonly="1"/><field name="total_discounts" readonly="1"/>
                    <field name="total_adjustments" readonly="1"/><field name="total_amount" readonly="1"/>
                </group>
                <field name="line_ids" readonly="1">
                    <list><field name="description"/><field name="quantity"/><field name="unit_price"/><field name="subtotal"/></list>
                </field>
                <footer>
                    <button name="action_confirm" type="object" string="Confirm &amp; Generate Invoice" class="btn-primary"/>
                    <button name="action_discard" type="object" string="Discard" class="btn-secondary"/>
                </footer>
            </form>
        </field>
    </record>
    <record id="view_tuition_invoice_confirm_wizard_form" model="ir.ui.view">
        <field name="name">tuition.invoice.confirm.wizard.form</field>
        <field name="model">tuition.invoice.confirm.wizard</field>
        <field name="arch" type="xml">
            <form string="Duplicate Invoice Warning">
                <div class="alert alert-warning" role="alert">
                    <strong><i class="fa fa-exclamation-triangle"/> Duplicate Invoice Warning</strong>
                    <p>An invoice already exists for this billing period.</p>
                </div>
                <group>
                    <field name="existing_invoice_name" readonly="1"/>
                    <field name="existing_invoice_date" readonly="1"/>
                    <field name="existing_invoice_amount" readonly="1"/>
                    <field name="existing_invoice_state" readonly="1"/>
                </group>
                <footer>
                    <button name="action_proceed" type="object" string="Generate Anyway" class="btn-warning"/>
                    <button name="action_cancel" type="object" string="Cancel" class="btn-secondary"/>
                </footer>
            </form>
        </field>
    </record>
    <record id="view_tuition_adjustment_wizard_form" model="ir.ui.view">
        <field name="name">tuition.adjustment.wizard.form</field>
        <field name="model">tuition.adjustment.wizard</field>
        <field name="arch" type="xml">
            <form string="Add Adjustment">
                <group>
                    <field name="adjustment_type"/><field name="description"/>
                    <field name="amount"/><field name="date"/>
                </group>
                <footer>
                    <button name="action_confirm" type="object" string="Add Adjustment" class="btn-primary"/>
                    <button string="Cancel" special="cancel" class="btn-secondary"/>
                </footer>
            </form>
        </field>
    </record>
    <record id="view_tuition_plan_change_wizard_form" model="ir.ui.view">
        <field name="name">tuition.plan.change.wizard.form</field>
        <field name="model">tuition.plan.change.wizard</field>
        <field name="arch" type="xml">
            <form string="Schedule Plan Change">
                <group>
                    <field name="product_id"/><field name="price"/>
                    <field name="start_date"/><field name="notes"/>
                </group>
                <footer>
                    <button name="action_confirm" type="object" string="Confirm Plan Change" class="btn-primary"/>
                    <button string="Cancel" special="cancel" class="btn-secondary"/>
                </footer>
            </form>
        </field>
    </record>
</odoo>
