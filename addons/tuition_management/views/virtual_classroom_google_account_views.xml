<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_virtual_classroom_google_account_list" model="ir.ui.view">
        <field name="name">virtual.classroom.google.account.list</field>
        <field name="model">virtual.classroom.google.account</field>
        <field name="arch" type="xml">
            <list string="Google Meet Accounts">
                <field name="sequence" widget="handle"/>
                <field name="name"/>
                <field name="host_email"/>
                <field name="active"/>
                <field name="last_checked_at"/>
            </list>
        </field>
    </record>

    <record id="view_virtual_classroom_google_account_form" model="ir.ui.view">
        <field name="name">virtual.classroom.google.account.form</field>
        <field name="model">virtual.classroom.google.account</field>
        <field name="arch" type="xml">
            <form string="Google Meet Account">
                <sheet>
                    <group>
                        <field name="name"/>
                        <field name="sequence"/>
                        <field name="active"/>
                        <field name="host_email"/>
                    </group>
                    <group string="Diagnostics">
                        <field name="last_checked_at"/>
                        <field name="last_check_error"/>
                    </group>
                </sheet>
            </form>
        </field>
    </record>

    <record id="action_virtual_classroom_google_accounts" model="ir.actions.act_window">
        <field name="name">Google Meet Accounts</field>
        <field name="res_model">virtual.classroom.google.account</field>
        <field name="view_mode">list,form</field>
    </record>
</odoo>
